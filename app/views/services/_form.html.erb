<%= form_with model: service, class: "space-y-5" do |f| %>
  <% if service.errors.any? %>
    <div class="rounded-md bg-red-50 px-4 py-3 text-sm text-red-800">
      <p class="font-medium">Please fix the following:</p>
      <ul class="mt-1 list-disc list-inside">
        <% service.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :service, "Type of service", class: "block text-sm font-medium text-stone-700" %>
    <%= f.text_field :service, class: "mt-1 block w-full rounded-md border-stone-300 shadow-sm focus:border-orange-500 focus:ring-orange-500" %>
  </div>

  <div>
    <%= f.label :location, "Property / apartment", class: "block text-sm font-medium text-stone-700" %>
    <%= f.text_field :location, class: "mt-1 block w-full rounded-md border-stone-300 shadow-sm focus:border-orange-500 focus:ring-orange-500" %>
  </div>

  <div>
    <%= f.label :address, class: "block text-sm font-medium text-stone-700" %>
    <%= f.text_field :address, class: "mt-1 block w-full rounded-md border-stone-300 shadow-sm focus:border-orange-500 focus:ring-orange-500" %>
  </div>

  <div>
    <%= f.label :contact_info, "Phone number", class: "block text-sm font-medium text-stone-700" %>
    <%= f.text_field :contact_info, class: "mt-1 block w-full rounded-md border-stone-300 shadow-sm focus:border-orange-500 focus:ring-orange-500" %>
  </div>

  <div>
    <%= f.label :description, class: "block text-sm font-medium text-stone-700" %>
    <%= f.text_area :description, rows: 4, class: "mt-1 block w-full rounded-md border-stone-300 shadow-sm focus:border-orange-500 focus:ring-orange-500" %>
  </div>

  <% if current_client.admin? && service.persisted? %>
    <div>
      <%= f.label :status, class: "block text-sm font-medium text-stone-700" %>
      <%= f.select :status, Service.aasm.states.map(&:name).map(&:to_s), {}, class: "mt-1 block w-full rounded-md border-stone-300 shadow-sm focus:border-orange-500 focus:ring-orange-500" %>
    </div>
  <% end %>

  <div class="flex items-center gap-3">
    <%= f.submit class: "rounded-md bg-orange-600 px-4 py-2 text-sm text-white hover:bg-orange-700 cursor-pointer" %>
    <%= link_to "Cancel", services_path, class: "text-sm text-stone-600 hover:text-stone-900" %>
  </div>
<% end %>
